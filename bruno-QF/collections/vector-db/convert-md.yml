info:
  name: convert-md
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/v1/vector-db/convert
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
