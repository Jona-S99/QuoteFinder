info:
  name: embedding-models
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/vector-db/embedding-models
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
