info:
  name: clean-data
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/api/v1/vector-db/clean
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
