info:
  name: create-db
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/api/v1/vector-db/build
  body:
    type: json
    data: |-
      {
        "embedding_model": "bge-m3:latest"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
