info:
  name: retrieval
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/v1/retrieval/query
  body:
    type: json
    data: |-
      {
        "query": "Riesgos de la IA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
